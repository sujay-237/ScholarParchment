'use client';

import React from 'react';

// Demo role switcher is removed per requirements.
// Authentication is now strictly handled via the Sign-In page (/auth).
export const DemoRoleSwitcher: React.FC = () => {
  return null;
};
